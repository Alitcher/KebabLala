using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TableHandler : MonoBehaviour
{
    [SerializeField] private RectTransform LeftMixture;
    [SerializeField] private RectTransform RightMixture;

    [SerializeField] private RectTransform[] DrinksMixture;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
